apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-pv-vol2
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: '/home/ubuntu/db-pv-vol2'
